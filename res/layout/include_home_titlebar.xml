<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rl_titlebar"
    android:layout_width="match_parent"
    android:layout_height="48dp"
    android:background="@color/bshoppingcolor"
    android:orientation="horizontal" >

    <TextView
        android:id="@+id/titlebar_tv_city_left"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:drawableRight="@drawable/ic_arrow_white_down"
        android:gravity="center_vertical"
        android:paddingLeft="16dp"
        android:paddingRight="16dp"
        android:text="珠海"
        android:textColor="@color/white"
        android:textSize="@dimen/txtsize_title" />

    <!-- <ImageView -->
    <!-- android:id="@+id/titlebar_iv_city_left" -->
    <!-- android:layout_width="wrap_content" -->
    <!-- android:layout_height="match_parent" -->
    <!-- android:gravity="center_vertical" -->
    <!-- android:paddingLeft="16dp" -->
    <!-- android:paddingRight="16dp" -->
    <!-- android:src="@drawable/ic_action_map" /> -->


    <!-- <TextView -->
    <!-- android:id="@+id/titlebar_tv" -->
    <!-- style="@style/TextViewTitle" -->
    <!-- android:layout_centerInParent="true" -->
    <!-- android:gravity="center" -->
    <!-- android:maxWidth="224dp" -->
    <!-- android:text="sssss" -->
    <!-- /> -->


    <!-- <EditText -->
    <!-- android:id="@+id/titlebar_et_search" -->
    <!-- android:layout_width="200dp" -->
    <!-- android:layout_height="30dp" -->
    <!-- android:layout_centerVertical="true" -->
    <!-- android:layout_toRightOf="@+id/titlebar_tv_city_left" -->
    <!-- android:background="@drawable/shape_search" -->
    <!-- android:drawableLeft="@drawable/ic_action_search" -->
    <!-- android:drawablePadding="5dp" -->
    <!-- android:hint="输入商家、品类、商圈" -->
    <!-- android:singleLine="true" -->
    <!-- android:textColor="@color/white" -->
    <!-- android:textSize="14dp" /> -->

    <ImageView
        android:id="@+id/titlebar_iv_search"
        android:layout_width="200dp"
        android:layout_height="30dp"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:src="@drawable/home_top_search" />

    <ImageView
        android:id="@+id/titlebar_iv_zxing_right"
        android:layout_width="48dp"
        android:layout_height="match_parent"
        android:layout_toRightOf="@+id/titlebar_iv_search"
        android:padding="12dp"
        android:src="@drawable/ic_action_favorite_off" />

    <ImageView
        android:id="@+id/titlebar_iv_msg_right"
        android:layout_width="48dp"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:padding="12dp"
        android:src="@drawable/ic_action_map" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1px"
        android:layout_alignParentBottom="true"
        android:background="@color/bg_agray" />

</RelativeLayout>